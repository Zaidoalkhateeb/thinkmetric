# Optional: URL of a backend endpoint (e.g. a form-handling service or your
# own API route) that accepts a POST request with JSON matching the contact
# form fields. If this is not set, the contact form falls back to opening a
# pre-filled email draft to the correct ThinkMetric address instead.
VITE_CONTACT_FORM_ENDPOINT=
